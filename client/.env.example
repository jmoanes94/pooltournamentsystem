# Local dev: optional (defaults to http://localhost:3001).
# Vercel / production: REQUIRED — public https URL of your Node Socket.io server (not this static site).
# Example: VITE_SOCKET_URL=https://pool-api.onrender.com
VITE_SOCKET_URL=http://localhost:3001
